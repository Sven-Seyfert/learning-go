package packageone

var PackageLevelVar = "This is a package level variable, only for the \"packageone\" package."

func PublicFuncOfPackageone() string {
	return "That's a text of a public function of packageone."
}
